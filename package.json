{
  "name": "pty-bridge",
  "version": "1.3.0",
  "private": true,
  "description": "PTY bridge — manage interactive terminal sessions via CLI",
  "scripts": {
    "build": "tsc",
    "vendor": "rm -rf skill/node_modules skill/package-lock.json && cd skill && npm install --force --no-package-lock",
    "prepare-skill": "npm run build && npm run vendor"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/briqt/pty-bridge.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
